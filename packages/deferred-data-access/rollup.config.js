import config from '../../rollup.helpers';

export const LIBRARY_FILE_NAME = 'deferred-data-access'; // dummy, replace with project name
export const LIBRARY_VAR_NAME = 'DeferredDataAccess'; // dummy, replace with project name

export default config(LIBRARY_FILE_NAME, LIBRARY_VAR_NAME);

import config from '../../rollup.helpers';

const LIBRARY_FILE_NAME = 'deferred-data-access'; // dummy, replace with project name
const LIBRARY_VAR_NAME = 'DeferredDataAccess'; // dummy, replace with project name

export default config(LIBRARY_FILE_NAME, LIBRARY_VAR_NAME);

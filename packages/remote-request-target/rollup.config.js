import config from '../../rollup.helpers';

const LIBRARY_FILE_NAME = 'remote-request-target'; // dummy, replace with project name
const LIBRARY_VAR_NAME = 'RemoteRequestTarget'; // dummy, replace with project name

export default config(LIBRARY_FILE_NAME, LIBRARY_VAR_NAME);

import config from '../../rollup.helpers';

export const LIBRARY_FILE_NAME = 'remote-resource-converter'; // dummy, replace with project name
export const LIBRARY_VAR_NAME = 'RemoteResourceConverter'; // dummy, replace with project name

export default config(LIBRARY_FILE_NAME, LIBRARY_VAR_NAME);

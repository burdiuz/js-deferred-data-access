import config from '../../rollup.helpers';

const LIBRARY_FILE_NAME = 'remote-resource-pool'; // dummy, replace with project name
const LIBRARY_VAR_NAME = 'RemoteResourcePool'; // dummy, replace with project name

export default config(LIBRARY_FILE_NAME, LIBRARY_VAR_NAME);
